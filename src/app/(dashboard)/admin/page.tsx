import { BlogCard } from "@/features/dashboard/blog-card";

const Dashboard = () => {
  return (
    <main className="min-h-screen flex items-center justify-center">
      <BlogCard />
    </main>
  );
};

export default Dashboard;
